To use this tool 

Start by running the generator.py script it will generate the values of n,e,d

The encryptor will get values of e,n

The decryptor will get values of d,n

We can encrypt and decrypt messages accordingly!!